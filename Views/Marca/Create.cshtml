@model Parcial2.ViewModels.MarcaCreateViewModel

@{
    ViewData["Title"] = "Create";
    @* List<Alfajor> alfjrList = (List<Alfajor>)ViewData["Alfjors"]; *@
}

<h1>Create</h1>

<h4>Marca</h4>
<hr />
<div class="row">
    <div class="col-md-4">
        <form asp-action="Create">
            <div asp-validation-summary="ModelOnly" class="text-danger"></div>
            <div class="form-group">
                <label asp-for="NombreMarca" class="control-label"></label>
                <input asp-for="NombreMarca" class="form-control" />
                <span asp-validation-for="NombreMarca" class="text-danger"></span>
            </div>
            <div class="form-group">
                <label asp-for="PaisMarca" class="control-label"></label>
                <input asp-for="PaisMarca" class="form-control" />
                <span asp-validation-for="PaisMarca" class="text-danger"></span>
            </div>
             @* @foreach (var item in alfjrList)
            {
                <div>@item.NombreAlfajor</div>
            }  *@
            <div class="form-group">
                <input type="submit" value="Create" class="btn btn-primary" />
            </div>
        </form>
    </div>
</div>

<div>
    <a asp-action="Index">Back to List</a>
</div>

@section Scripts {
    @{await Html.RenderPartialAsync("_ValidationScriptsPartial");}
}
