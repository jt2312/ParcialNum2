@model IEnumerable<Parcial2.Models.Marca>

@{
    ViewData["Title"] = "Index";
}

<h1>Index</h1>

<p>
    <a asp-action="Create">Create New</a>
</p>
<table class="table">
    <thead>
        <tr>
            <th>
                @Html.DisplayNameFor(model => model.NombreMarca)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.PaisMarca)
            </th>
            <th></th>
        </tr>
    </thead>
    <tbody>
@foreach (var item in Model) {
        <tr>
            <td>
                @Html.DisplayFor(modelItem => item.NombreMarca)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.PaisMarca)
            </td>
            <td>
                <a asp-action="Edit" asp-route-id="@item.MarcaID">Edit</a> |
                <a asp-action="Details" asp-route-id="@item.MarcaID">Details</a> |
                <a asp-action="Delete" asp-route-id="@item.MarcaID">Delete</a>
            </td>
        </tr>
}
    </tbody>
</table>
